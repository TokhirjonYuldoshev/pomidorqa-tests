# Карта тестового покрытия

Документ связывает основные риски проекта с конкретными test suites. Он нужен для навигации по репозиторию, ревью изменений и объяснения покрытия на техническом собеседовании.

Источник истины — код в `main`. Если меняется структура тестов или зона ответственности spec-файла, эта карта обновляется в том же Pull Request.

## Принципы покрытия

- Unit проверяет детерминированную логику без браузера и live-стенда.
- API проверяет локальные HTTP-контракты.
- E2E используется для интегрированных пользовательских сценариев PomidorQA.
- Live E2E выполняется в Chromium, Firefox и WebKit с `workers=1` и `retries=0`.
- Тестовые данные уникальны; для setup предпочтителен API, если UI-регистрация не является предметом проверки.
- Созданные тестовые аккаунты удаляются централизованным teardown.

## Unit

| Файл | Область |
| --- | --- |
| `tests/unit/auth.spec.ts` | детерминированная логика авторизации |
| `tests/unit/slots.spec.ts` | детерминированная логика свободных слотов |

## API

| Файл | Область |
| --- | --- |
| `tests/api/booking-api.spec.ts` | HTTP-контракты бронирования |
| `tests/api/participants-api.spec.ts` | HTTP-контракты участников |

## E2E

### Авторизация и сессии

| Файл | Основной риск |
| --- | --- |
| `tests/e2e/login-error.spec.ts` | отрицательный сценарий входа и обработка ошибки |
| `tests/e2e/auth-session.spec.ts` | сессия, logout, защищённые страницы и изоляция браузерных контекстов |

### Профиль и свободные слоты

| Файл | Основной риск |
| --- | --- |
| `tests/e2e/profile-flow.spec.ts` | сохранение профиля, навыки, удаление и изоляция данных аккаунтов |
| `tests/e2e/slots-management.spec.ts` | создание, сохранение, несколько дат/времён и изоляция слотов |

### Каталог и поиск

| Файл | Основной риск |
| --- | --- |
| `tests/e2e/catalog-search.spec.ts` | базовый положительный/отрицательный поиск, self-exclusion и правила видимости |
| `tests/e2e/catalog-search-data.spec.ts` | несколько навыков/слотов, дубли карточек, общие навыки и типы навыков |
| `tests/e2e/catalog-search-semantics.spec.ts` | пробелы, регистр, частичное совпадение, пустой запрос, Enter и семантика фильтра |
| `tests/e2e/catalog-search-boundaries.spec.ts` | граничные значения и специальные данные строки поиска |
| `tests/e2e/catalog-search-edge-data.spec.ts` | редкие сочетания данных каталога и дополнительные граничные случаи |
| `tests/e2e/catalog-search-state.spec.ts` | обновление каталога после изменения состояния профиля и связанных данных |
| `tests/e2e/catalog-lifecycle-cascades.spec.ts` | жизненный цикл аккаунта и каскадные изменения связанных сущностей |

### Бронирование и доступность

| Файл | Основной риск |
| --- | --- |
| `tests/e2e/booking-flow.spec.ts` | основной путь бронирования и гонка двух пользователей за один слот |
| `tests/e2e/booking-cancel.spec.ts` | отмена встречи и согласованность состояния у обеих сторон |
| `tests/e2e/catalog-booking-availability.spec.ts` | изменение видимости участника при занятии свободных слотов |
| `tests/e2e/catalog-booking-recovery.spec.ts` | восстановление доступности после отмены и повторное бронирование |

## Visual Regression

| Файл | Область |
| --- | --- |
| `tests/visual/public-pages.visual.spec.ts` | визуальный эталон публичных страниц, используемый отдельным workflow |

## Нефункциональные проверки вне spec-файлов

Отдельные GitHub Actions workflows покрывают другие классы риска:

- Accessibility Audit — axe-core / WCAG;
- Performance Smoke / Lighthouse — производительность публичных страниц;
- Security & Quality Gates — зависимости, SBOM и статические проверки;
- Nightly E2E Regression — плановая проверка внешнего стенда;
- Stability Check — повторные прогоны без retries;
- Registration Contract Smoke — ручная проверка точного HTTP-контракта регистрации;
- Telegram Notification Test — диагностика канала уведомлений.

## Как использовать карту

При добавлении нового теста сначала определить риск и минимально достаточный уровень. Новый E2E нужен только тогда, когда риск нельзя надёжно закрыть Unit/API-проверкой или уже существующим пользовательским сценарием.

При ревью важно проверять не количество новых тестов, а отсутствие дублирования, изоляцию данных, корректный cleanup и понятную связь сценария с риском.